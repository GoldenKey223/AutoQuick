# AutoQuick ToDo list

- UI

    - Qt
    - logo
    - blue (vs code theme)

- network drive

    - --config should actually accept config
    - user to put root of networkdrive, path, username, password
    - encryption

- additional functions

    - --list: list current timetable with index
    - --add/remove: remove with index
    - --help: show detailed help when used with other options (AutoQuick --help --config)

- priority

    - network drive